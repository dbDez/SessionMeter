# HandOff — <project> — <YYYY-MM-DD>

> Written before a context/rate wall so a fresh session can `carry on` losslessly.
> Keep it at the working-dir root and commit it — this file is the memory, not the chat scrollback.

## Decisions made
- (choices locked in this session — so the next session doesn't re-litigate them)

## What was done
- (with file paths and/or commit hashes)

## Lessons learned
- (gotchas worth keeping — the durable notes that must outlive the context clear)

## Outstanding
1. (next actions, in priority order, with file paths)
2.

## First action next time
- (the single thing to do first when you `carry on`)
